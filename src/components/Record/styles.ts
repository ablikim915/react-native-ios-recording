import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  recorder: {
    marginBottom: 30
  },
  recordTime: {
    fontSize: 24,
    color: '#333',
    marginVertical: 16,
    height: 30,
    textAlign: 'center'
  },
  recordButton: {
    borderRadius: 64,
    flexDirection: 'row',
    justifyContent: 'space-between'
  }
});